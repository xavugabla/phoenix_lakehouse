# Minimal requirements for Prefect work pool
# Focused on lakehouse audits, migrations, and catalog sync tasks

# Core orchestration + serialization
prefect>=2.14.0
pandas>=2.0.0,<3.0.0
pyarrow>=14.0.0
PyYAML>=6.0
structlog>=23.1.0
pydantic>=2.0.0
pyiceberg[gcp,pandas,pyarrow]>=0.5.0

# Google Cloud dependencies
google-cloud-storage>=2.10.0
google-cloud-bigquery>=3.11.0
google-auth>=2.23.0

